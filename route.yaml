apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: sber-monitoring-route
  namespace: kovaliov2700-dev
spec:
  to:
    kind: Service
    name: sber-monitoring-service # Имя сервиса, который создали на шаге 2
    weight: 100
  port:
    targetPort: 3000
