apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: grafana-route
  namespace: kovaliov2700-dev
spec:
  to:
    kind: Service
    name: grafana
  port:
    targetPort: http
  tls:
    termination: edge
  wildcardPolicy: None
